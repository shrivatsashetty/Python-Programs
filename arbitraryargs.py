def operations(*numbers): 
    # a tuple numbers is created and arg are passed into it
    print(f"the sum of {numbers[0]}+{numbers[1]} is {numbers[0]+numbers[1]}")

operations(1,2,3,4) # calling the function with multiple args